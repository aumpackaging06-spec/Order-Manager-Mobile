kind = "web"
previewPath = "/"
title = "AUM Packaging"
version = "1.0.0"
id = "artifacts/aum-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21055

[services.development]
run = "pnpm --filter @workspace/aum-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aum-app", "run", "build" ]
publicDir = "artifacts/aum-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21055"
BASE_PATH = "/"
